spring:
  application:
    name: tags

youtube:
  api:
    key: ${YOUTUBE_API_KEY}
    base:
      url: https://www.googleapis.com/youtube/v3
    max:
      related:
        videos: 5

server:
    port: 8081